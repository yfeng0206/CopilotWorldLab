[pytest]
# Only discover this project's tests; the vendored third_party/vjepa2 ships its own
# test suite whose imports are not installed here and must not be collected.
testpaths = tests
norecursedirs = third_party .venv checkpoints logs outputs data
